import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RegistrAPP-DUOC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
